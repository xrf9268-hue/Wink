# Handoff Notes

On 2026-08-15, issue #438 added a real macOS Focus Filter App Intents extension with stable UUID-backed Profile entities, an App Group catalog/state contract that operates while Wink is not running, and a main-app coordinator that reconciles the durable file at startup and on Darwin wake-up notifications. A Focus Profile is an overlay over the latest manual base: Focus wins while active, manual changes update only the base, and deactivation restores that exact base through #437's validate/commit/apply transaction before clearing it. Reconciliation compare-checks the shared snapshot and holds the same cross-process lock through synchronous pause/Profile application and restore-marker commit, preventing Focus B or deactivation from overtaking an in-flight Focus A apply; state mutations, destructive catalog invalidation, and the restored active pointer cross stable-storage barriers before their corresponding retry/deletion markers can clear, and transient profile-apply, profile-restore, and shared-state failures use a bounded retry ladder while catalog repair retriggers reconciliation. Profile deletion and extension selection are also serialized under that lock, so either the durable Focus reference blocks deletion or catalog invalidation makes a later selection fail. Automatic switches defer across recovery or unsaved editor work; stale/deleted references fail explicitly without fallback. Focus pause is a third independent runtime bit composed with manual and frontmost-exception pause and adds no TCC or Screen Recording permission. Packaging now embeds and signs `WinkFocusIntentsExtension.appex`, separately extracts its metadata, and applies the same `group.com.wink.app` entitlement to host and sandboxed extension; because App Groups, App Sandbox, and Hardened Runtime are unrestricted macOS entitlements, Developer ID mode validates the finished nested signatures directly and requires no provisioning profile unless a future restricted entitlement is added. Unit/integration/localization coverage is present; final exact-tree packaged Focus appearance, running/not-running delivery, nested codesign/App Group, and ordinary E2E runtime validation are still pending.

## Current State
On 2026-08-14, issue #435 made menu shortcut rows and Insights suggestions actionable without adding a parallel palette or synthetic-input path. Menu rows are plain accessible buttons for mouse, Full Keyboard Access, and VoiceOver; they pass the exact saved shortcut UUID through `ShortcutManager.trigger(source: .menuBar)` into the unchanged AppSwitcher cooldown, re-entry, frontmost-behavior, and asynchronous confirmation pipeline. Capture transport readiness (Carbon, EventTap, standard Fn observer, Secure Input, and capture pause) does not gate a direct click, while enabled/installed availability and current Accessibility trust still fail closed with inline feedback. Every accepted asynchronous action returns a unique per-invocation token containing the concrete resolved bundle, attempt ID/generation, and activation-vs-deactivation phase; terminal activation and hide outcomes are retained long enough for the menu callback, frontmost-app pseudo-targets cannot adopt another app's session, profile switches plus `ShortcutStore`'s synchronously advanced configuration revision invalidate pending feedback without an Observation scheduling window, exact binding/availability changes clear only their owning row's displayed failure, unrelated row lifecycle changes retain it, and a nested rejected re-entry cannot overwrite the outer invocation receipt. Every failure stays inline and posts a high-priority accessibility announcement. Accepted actions keep the existing UUID for usage; rejected actions do not record usage; `SHORTCUT_INVOKE` diagnostics explicitly distinguish `source=menu` from physical capture and record the final `confirmed`/`failed` outcome. Insights suggestion buttons now seed the existing Shortcuts composer with exactly one app/bundle, clear any stale chord, and open Settings > Shortcuts exactly once even when Settings is already open on another tab; nothing persists until the user records and confirms a chord. English and Simplified Chinese copy plus focused menu/source/usage/suggestion/async-failure tests are included. **macOS runtime validation complete** on exact final executable SHA-256 `fcf7ae4b5338d282c3a582d8f893bc903a72645f4ea4c0e18a9531569ea67c31`: strict deep signing, localization drift, all 1,127 tests, release packaging, and packaged E2E 6/6 with 0 warnings passed. Computer Use confirmed the final package loaded an isolated Safari-disabled/IINA-enabled fixture in its real Settings UI; its window API could not expose SwiftUI's `MenuBarExtra`, so the final hash's popover was not falsely claimed as pointer-retested. The immediately preceding exact-package round retains real menu/Insights mouse and AX evidence for the unchanged action path, while the final ownership delta has deterministic focused coverage for unrelated-row retention plus both sides of the pre-Observation-task timing window. Application Support, diagnostics, and defaults were restored to matching digests after validation.

On 2026-08-14, issue #434 added an optional first-shortcut onboarding guide inside Settings > Shortcuts. Fresh installs are introduced without persisting completion; existing users are never forced into the guide; only explicit Skip or a verified real shortcut completes it. The guide reuses the existing picker and recorder, requests only the saved shortcut's actual transport permissions (standard, standard Fn+F-row, or Hyper), waits for exact per-chord route readiness, and accepts only the precise AppSwitcher activation attempt ID and generation created by the matching physical press. Unrelated same-bundle activations, unavailable targets, rejected dispatch, target-already-frontmost hides, window-picker anti-reraise promotion, edits, deletion, and timeouts fail closed. Pending attempts survive relaunch; every whole shortcut-set apply synchronously reconciles ownership even when the Shortcuts tab is unmounted; recovery placeholders never masquerade as a fresh empty install; matched and terminal states survive transient exception auto-pause; and the verification deadline covers AppSwitcher's full production activation ceiling. English and Simplified Chinese copy plus focused state/editor/manager/controller/layout coverage are included. **macOS runtime validation complete** for the final post-review tree: the signed packaged executable (`13f9492e7d99933b580925918607451e358ee77c81136026149a4dc5a05efa65`) passed strict deep codesign and the packaged E2E suite 6/6 with 0 warnings; live Settings validation confirmed manual-guide gating, Current App exclusion, direct-activation rejection, and an exact Carbon `⌥⌘Y` activation chain (`matched` → `TOGGLE_STABLE` → `confirmed`) before the success UI. The isolated Application Support, defaults, and debug-log state was restored and rechecked. Physical standard Fn+F-row proof remains under the repository's separate hardware-only Fn gate because AppleScript cannot synthesize physical Fn.
On 2026-08-13, issue #433 extended the non-destructive `wink://` surface with Search Palette, typed-tab Settings, and installed-app `focus`. `AppDelegate` now reads the raw `kAEGetURL` Apple Event direct object so Foundation cannot normalize Unicode host aliases before validation; `WinkURLCommand` applies the pure fail-closed `URLComponents` grammar with explicit size and ASCII bundle-id bounds while retaining legacy toggle's outer-whitespace normalization and installed legacy identifiers containing `_`. `WinkURLCommandHandler` validates installed targets, canonicalizes their bundle identifiers from the resolved app bundle, never logs raw rejected input, deduplicates Search and Settings within a delivery batch, and forces `focus` through AppSwitcher's idempotent `.focus` override instead of hide/cycle lanes. An already-stable frontmost focus returns without a delayed confirmation, and direct focus replaces older degraded attempts so retry ceilings cannot silently swallow it. If focus supersedes a dispatched toggle-off, a short fast-settlement observation catches the usual asynchronous hide while one-shot ownership of the matching process's `didHide` remains armed through a separate two-second focus-intent window; after expiry a later manual/self-hide wins. Termination, a newer Wink hide, or a newer PID-distinct foreground activation while the recovering target remains unhidden also cancels recovery. A PID-distinct foreground candidate is recorded before the next-main-run-loop hidden-state check, so an old `didHide` landing inside that gap cannot steal focus back; an already-hidden target still identifies the system's automatic foreground handoff. Before the delayed candidate cancels recovery it must still match `NSWorkspace.frontmostApplication` by PID, so stale notification delivery cannot override a target that already regained the foreground. Reactivation of the recovering PID also cancels an obsolete foreground candidate, and explicit focus attempts carry distinct intent generations, preventing delayed callbacks from overwriting current workspace state or a newer renewed focus. A confirmed PID-distinct foreground choice also idles only the exact activation generation bound to that focus, so its still-scheduled confirmation ladder cannot steal the foreground back while newer generations remain untouched. Cold-launch Settings delivery uses the existing queueable `SettingsLauncher` commit while result-bearing App Intents retain their ready-and-observed success contract. Parser, raw-delivery, handler, executor, focus-override, and dispatched-hide race coverage plus English/Chinese guide and architecture updates are complete. Exact implementation `33d19979fff35f7af4d3202598277796752af4bb` was rebuilt and validated at `/Users/yvan/developer/Wink/build/Wink.app` (executable SHA-256 `9576a5efe5080ad993af4eb06f50a26b04de5dae8e134a011a692002bfa21668`): 1,073 tests passed, codesign passed, packaged E2E passed 6/6 with 0 warnings on the 18-shortcut all-Hyper fixture, and exact-bundle cold-launch, URL delivery/deduplication/rejection, canonicalized focus, and secret-redaction probes passed. In five consecutive real `toggle -> focus -> activate Firefox` rounds, Firefox remained frontmost and Image Capture remained visible; because this host's `hide()` returned false, the exact dispatched-hide generation branch remains deterministically covered by tests rather than claimed as directly observed. Runtime validation is complete; no callback/import/file/script/permission/preference surface was added, and no privacy/security setting was changed during validation.
On 2026-08-13, issue #432 added a deterministic App Intents surface for `Pause Wink`, `Resume Wink`, `Show Wink Search Palette`, and typed-tab `Open Wink Settings`. `WinkActionExecutor` is the single main-actor path for equivalent menu-bar, URL, shortcut-trigger, and App Intent actions: pause/resume verifies the manual bit without collapsing exception auto-pause, palette presentation is idempotent and verified, and App Intent settings requests use `SettingsLauncher.openIfReady` plus an observed visible-window timeout so an unready SwiftUI bridge or declined presentation returns an explicit error instead of a queued false success. macOS 15-25 uses `openAppWhenRun`; macOS 26+ advertises foreground-dynamic `supportedModes`. Because plain SwiftPM does not run Xcode's extraction phase, packaging invokes `scripts/extract-app-intents-metadata.sh` and fails unless the final bundle contains four actions, four SF Symbol-backed App Shortcuts, and English/Simplified Chinese phrase catalogs. Exact-head packaged Shortcuts.app validation completed at `05429fdbdf1a58152cd28bedb3d8213422ab29df` against `/Users/yvan/developer/Wink/build/Wink.app` (executable SHA-256 `d74ad5471ebd57a686561df4485fa49f6cabc170d037cf1c714fa66a5f3ca71c`): all four actions, typed Settings enum, English/Simplified Chinese discovery, pause/resume state, search presentation, and Insights navigation were observed. No new TCC permission was introduced.
On 2026-07-21, the AltTab-integration wave landed 10 of its 12 planned issues (#347–#358) as PRs #359–#362, #364–#366, #369–#370, and #372, all admin-merged after CI green plus codex-bot review settling. The wave grew from a 16-agent research workflow over the alt-tab-macos source (verified roadmap and rejections are recorded in the batch issues themselves): window cycling as a fourth `FrontmostTargetBehavior` with a wid-identity cursor and the current alt-tab/yabai `makeKeyWindow` byte layout (0x100 buffer, mouse down/up pair, off-window (-1,-1) point), per-shortcut behavior overrides with deliberately lenient decoding (unknown values must degrade to nil, never quarantine `shortcuts.json`), a frontmost-app pseudo-target behind the sentinel bundle id `wink.target.frontmost-app` (recipes carrying it export as schema v2; plain recipes stay v1), a display-only cycle HUD anchored to the cycled window's display, per-app exception rules composing with manual pause by OR in `ShortcutManager`, Secure Input surfacing via a lazy `IsSecureEventInputEnabled()` probe on the existing 3s poll, local-only app-activation counting behind an additive `app_activations` table (deliberately outside the `user_version` scheme; the privacy toggle stops AND clears, including a disabled-startup sweep), a `wink://` URL scheme (`open -g` required — a plain `open` activates Wink and makes toggle-off unreachable), and a hold-Hyper cheat sheet fed by a display-only `EventTapBox.onHyperHoldEvent` seam (began/chordConsumed/ended, including the flagsChanged delivery shape; FIFO main-queue hop; resets on hyper-disable and on any composed-pause transition; enabled gate requires `eventTapActive` so an all-standard configuration cannot advertise a dead gesture). Two recurring CI-toolchain crashes were root-caused to complex init default arguments (Xcode 16.4 / Swift 6.1.2 SILGen): new injectable init parameters must use `param: T? = nil` resolved in the body. Automated batch validation is complete on the packaged 0.6.2 `build/Wink.app` (binary sha256 f6d8a235…): `bash scripts/e2e-full-test.sh` passed 6/6 with 0 warnings on the user's 17-shortcut all-Hyper fixture, URL-scheme smoke passed against debug.log traces, the real `usage.db` gained `app_activations` additively with `user_version` still 3, and TCC grants survived repackaging under the stable signing identity. Physical validation is tracked as issue #371 (cycle feel at the 150ms gate, HUD display anchoring, override routing, Current App picker, exception-rule pill copy, Secure Input pill, plus #372's idle-hold items); merged wave PRs keep `macOS runtime validation pending` until #371 completes. Remaining wave work: #358 localization (investigate first — `.xcstrings` auto-extraction is an Xcode build feature, plain `swift build` does not sync catalogs) and #352 hold-to-show window picker plus #356 search palette, both intentionally gated behind #371's physical confirmation of the cycle-family activation path.
On 2026-04-26, the app logo assets were realigned with the latest `docs/design/Wink Logo Options.html` and `docs/design/Wink UI v2 (standalone).html` direction. The live SwiftUI `Logo_WinkTwin` implementation now uses the same 32u circle-subtraction crescent + open-dot geometry as `AppIcon.svg` and `MenuBarTemplate.svg`; `WinkAppIcon`, `AppIcon.svg`, `AppIcon.icns`, and the DMG background now use the current v2 violet-to-blue anchor gradient. The old SwiftUI arc implementation, old app-icon palette, stale DMG palette, and unused v2 design-source `Logo_WinkCrescent` compatibility aliases were removed. Regression coverage now samples rendered SwiftUI pixels for the Twin crescent geometry and app-icon gradient, and checks the live SVG/DMG assets for the current palette so future logo updates cannot leave stale generated assets behind.
On 2026-04-26, the DMG installer background was further refreshed to the selected light editorial direction from `docs/design/Wink DMG Installer v2.html`: one install instruction, top-left copy, oversized low-contrast Wink Twin watermark, tapered motion arrow, and bottom-anchored real Finder icons. The design mock's empty toolbar strip was intentionally not shipped: real Finder toolbar chrome varies by system appearance and can introduce visible controls/sidebar behavior, so `scripts/package-dmg.sh` hides the toolbar, collapses the sidebar defensively, uses Finder-accurate 96px icons with 11px labels, and positions `Wink.app` / `Applications` at `{122,300}` / `{558,300}` in a `680x460` window. The `Applications` item is a Finder alias to `/Applications` with a custom design-matched folder icon, avoiding the default symlink arrow badge while preserving the drag target. The background asset is sized to the actual no-toolbar Finder content height (`680x429`) so the installer window has no bottom white gap. The old centered B-style copy and footer line were removed rather than kept as alternate paths; the dark F concept remains a design reference, not a shipped default. Local verification covered the focused logo/DMG tests, full `swift test` (341 tests), release build, packaged app rebuild, DMG rebuild, app codesign verification, `hdiutil verify build/Wink-0.3.0.dmg`, whitespace/residue scans, and mounted screenshot evidence at `build/validation/dmg-editorial-2026-04-26/finder-window-final.png`.
On 2026-04-26, issue #229 retired the stale `previousBundle` / `previousApp` toggle context. `ToggleSessionCoordinator` now owns only attempt identity, pid, phase, activation path, timing, and retry/degraded state; `PendingActivationState`, `PendingDeactivationState`, `StableActivationState`, `TOGGLE_TRACE_*`, and lifecycle logs no longer carry previous-app telemetry. This matches the current hide model: Wink requests `NSRunningApplication.hide()` and confirms the target is no longer foreground/visible, while macOS chooses the next foreground app. Validation covered focused regression tests for removed telemetry fields, the full Swift test suite, release build, packaged app rebuild, codesign verification, and packaged mixed-route E2E after re-granting TCC for the exact `/Users/yvan/developer/Wink/build/Wink.app` bundle (`bash scripts/e2e-full-test.sh`: `All 6 tests passed (0 warnings)`). The E2E log window stayed free of `previousBundle` / `previousApp` / `previous=` telemetry and had no blocking runtime trace signatures (`event=degraded`, `TOGGLE_HIDE_DEGRADED`, `phase=no_session`, `hide_untracked`, or `SHORTCUT_TRACE_BLOCKED`). Evidence is under `build/validation/issue-229-e2e-2026-04-26/`; physical held-key repeat testing beyond the repo debounce/loop-prevention modules was not separately performed.
On 2026-04-26, issue #228 fixed the blank selected menu bar placeholder that remained after the UI v2 icon migration. The app and menu bar SVG sources now use the Wink Twin mark, `scripts/build-app-icon.sh` has regenerated `AppIcon.icns` plus the 1x/2x menu bar template PNGs, and `WinkMenuBarScene` now uses `MenuBarExtra(isInserted:content:label:)` with a custom template label backed by an `NSImage` whose `isTemplate` flag is set explicitly. This keeps the menu bar image on AppKit's template tinting path for both closed and opened/highlighted states instead of relying on the higher-level image overload. Regression coverage now asserts the template image remains non-empty enough to avoid a blank slot and that the menu bar scene descriptor records the custom-label path. Local validation for this visual scope included the focused menu bar/icon tests, full `swift test`, packaged app rebuild, codesign verification, packaged runtime launch, AX inspection of the live `AXMenuExtra`, and closed/open screenshot crops of the menu bar item.
On 2026-04-23, issue #184 upgraded the DMG installer from a plain `Wink.app + Applications` volume into a branded Finder install surface. `scripts/package-dmg.sh` stages `assets/dmg/wink-dmg-background.png`, creates a writable HFS+ disk image, uses Finder scripting to save icon-view window chrome/layout into `.DS_Store`, and then converts the result back to the final compressed `build/Wink-<version>.dmg`. The package still uses only built-in macOS tooling and adds no CI/release dependency. The original visual treatment from this issue was superseded by the 2026-04-26 light editorial refresh above; treat that newer entry as the current DMG visual source of truth.
On 2026-04-22, issue #205 (Phase 4 of the UI v2 plan) finished the remaining UI v2 polish layer. The menu bar shell now uses the generated Wink Twin template image instead of the older SF Symbol bolt, `MenuBarPopoverView` filters shortcut rows with real `localizedCaseInsensitiveContains` search, and the Today histogram is backed by `UsageTracker.hourlyCounts(days: 1)` rather than the old averaged mock. Insights now shows KPI cards (Activations / Time saved / Streak), a 24x7 hourly heatmap, per-app sparklines plus delta badges, and a weekly unused-shortcut nudge. `UsageTracker` now persists both `daily_usage` and `usage_hourly`, exposes `hourlyCounts(days:relativeTo:)`, `previousPeriodTotal(days:relativeTo:)`, and `streakDays(relativeTo:)`, and `UsageDatabaseBootstrap` performs an explicit pre-startup reset when it detects a legacy usage database shape (including the older `usage_daily` path) instead of silently zeroing data. `scripts/build-app-icon.sh` now regenerates `AppIcon.icns` plus the menu bar template PNGs from the SVG sources, and packaged builds copy the SwiftPM resource bundle into `Wink.app` so the menu bar image loads from the app bundle at runtime. Verification for this issue included official Apple-doc review for `MenuBarExtra(image:)`, `NSImage.isTemplate`, Asset Catalog template rendering intent, and `iconutil`, plus SQLite `PRAGMA user_version` / `PRAGMA table_info` verification for the schema bootstrap path; local `swift test --no-parallel` (323 tests), `swift test` (323 tests), `swift build -c release`, and `bash scripts/package-app.sh` all passed. `bash scripts/e2e-full-test.sh` is still blocked on local TCC re-grant for the freshly packaged `build/Wink.app` (recent logs show `ax=false im=false carbon=false eventTap=false`), and manual macOS runtime validation is still pending for packaged-app permission re-grant + E2E rerun, light/dark menu bar template rendering, and Dock / Updates-card / menu bar visual consistency, so the issue remains `macOS runtime validation pending`.
On 2026-04-22, issue #204 (Phase 3 of the UI v2 plan) replaced the remaining AppKit menu bar shell with SwiftUI `MenuBarExtra("Wink", systemImage: "bolt.square.fill")` plus `.menuBarExtraStyle(.window)`. `WinkApp` now binds `menuBarIconVisible` directly to `MenuBarExtra.isInserted`, so General truthfully re-exposes `Show Menu Bar Icon` only now that the toggle actually inserts and removes the live scene. The new `MenuBarPopoverView` rebuilds the v2 shell with the wordmark/version header, Ready/Paused pill, placeholder search field, mock 24-bar Today histogram derived from the current day's `UsageTracker.dailyCounts`, shortcut rows backed by `ShortcutStore` + `ShortcutStatusProvider`, and `Manage…` / `Settings…` / `Check for Updates…` / `Quit Wink` actions. `Pause all shortcuts` is now persisted through `AppPreferences.shortcutsPaused`, routed through `ShortcutManager.setShortcutsPaused(_:)`, and stops both Carbon and Hyper capture without mutating `shortcuts.json`. The old `MenuBarController` / `MenuBarShortcutItemPresentation` / `MenuBarShortcutRowView` AppKit path and its tests were deleted outright rather than left as compatibility shims, and app reopen with no visible windows now reopens Settings as the recovery path when the menu bar icon has been hidden. Verification for this issue included official Apple-doc review for `MenuBarExtra`, `.menuBarExtraStyle(.window)`, `App`, `Scene`, and `openSettings`, a full local `swift test --no-parallel` pass (308 tests), `swift build -c release`, and `bash scripts/package-app.sh`. Manual macOS runtime validation is still pending for live popover behavior (open/close/outside click), `Pause all shortcuts` suppression and resume, `Show Menu Bar Icon` hide/show behavior, and the hidden-icon reopen path, so the issue remains `macOS runtime validation pending`.
On 2026-04-22, issue #203 (Phase 2 of the UI v2 plan) replaced the old custom settings-window path with a native SwiftUI settings shell. `WinkApp` now owns the `Settings` scene, `SettingsLauncher` bridges AppKit callers to `EnvironmentValues.openSettings`, and `SettingsView` now uses `NavigationSplitView` with shared tab selection for `Shortcuts`, `Insights`, and `General` instead of the older segmented shell. `SettingsWindowController.swift` was deleted outright rather than kept as a compatibility shim. The General tab now persists `frontmostTargetBehavior` (`hide` / `toggle` / `focus`) through `AppPreferences`, and that setting is wired through `ShortcutManager` into `AppSwitcher` / `ToggleSessionCoordinator` without changing the default `.toggle` behavior. Phase 2 intentionally does not expose a placeholder `Show Menu Bar Icon` control before the later menu-bar-shell migration gives that preference a real runtime effect. The bottom link row is now truthful as well: `Privacy` points at `docs/privacy.md` instead of the repo root. Verification for this issue includes official Apple-doc review for `Settings`, `openSettings`, `NavigationSplitView`, `Form`, and modern `NSApplication.activate()`, a full local `swift test --no-parallel` pass (305 tests), `swift build`, and `bash scripts/package-app.sh`. A focused packaged-app UI spot-check of the new Settings scene is still pending in this handoff, so the issue should continue to be treated as `macOS runtime validation pending` until someone manually launches the new `build/Wink.app` and exercises the Settings entry points on macOS.
On 2026-04-21, issue #180 upgraded the then-AppKit menu bar dropdown from static actions only to a lightweight shortcut overview panel. That historical `MenuBarController` implementation was later removed by issue #204, but the shortcut ordering/running/unavailable presentation semantics it introduced continue to live on through the SwiftUI `MenuBarPopoverView` + `ShortcutStatusProvider` path.
On 2026-04-21, issue #181 redesigned the Settings > Insights presentation to match the newer Shun-inspired direction: the old split period/48pt total/"app switches" header and intermediate usage chart are gone, replaced by a single natural-language summary banner plus a flatter Most Used section without the extra CardView title chrome. Ranking rows drop the decorative rank circles, and the usage bars now span the available row width with a thicker, visually consistent shape tuned against the reference screenshot. The change is code/test verified on the local macOS Swift toolchain (`swift test`, including layout regressions and `InsightsViewModelTests`), and the refreshed Insights tab received a same-day macOS visual spot-check on the packaged app.
On 2026-04-21, issue #182 simplified the Shortcuts list row presentation in Settings: `ShortcutsTabView` now hides visible bundle identifiers, promotes `N× past 7 days` to the primary subtitle, enlarges app icons to 36pt, and adds a little more vertical breathing room without changing shortcut controls, import/export behavior, or persistence. Verification for this issue now includes layout regression coverage, a same-day macOS visual spot-check of the Settings > Shortcuts list, and a packaged-app `bash scripts/e2e-full-test.sh` pass on the current standard-only fixture (`All 6 tests passed (1 warnings)`, with the Hyper module warning remaining the expected configuration skip).
On 2026-04-21, issue #175 was re-implemented from latest `main` instead of reviving stale PR #189. Wink now pulls in Sparkle 2.9.1 via SwiftPM, starts a programmatic updater only when `SUFeedURL` and `SUPublicEDKey` are present, exposes a truthful `Check for Updates…` section in General settings, and the packaging/release tooling now embeds and re-signs `Sparkle.framework`, builds `Wink-<version>.zip`, signs `appcast.xml`, and uploads DMG/ZIP/appcast artifacts to Cloudflare R2 from the release workflow. A same-day hardening pass moved the live `appcast.xml` upload to the final release step so Sparkle clients do not see a new update before GitHub Release publication succeeds, and PR CI now smoke-tests ZIP/appcast generation with temporary Sparkle keys plus an R2 upload dry run. Local code/package verification is complete; credential-backed Sparkle feed signing and notarized public-release validation are still pending real Apple, Sparkle, and R2 secrets.
On 2026-04-17, the PR governance baseline and deterministic review-state harness were added on the `codex/pr-governance-harness` line: `.github/workflows/review-gate.yml` now evaluates GitHub `reviewDecision` plus unresolved non-outdated inline review threads, `.github/scripts/validate-review-state.mjs` writes maintainable failure summaries instead of raw payload dumps, `.github/governance/main-ruleset.json` captures the desired `main` merge policy in-repo, and the supporting docs now distinguish repository-native review gates from separate macOS runtime-validation evidence. The remaining rollout step is administrative sequencing: merge the workflow/scripts to `main` first, then apply the checked-in ruleset so `Review Gate / Validate review state` exists before it becomes required. One important GitHub Actions caveat is now documented explicitly: Actions can rerun this check on PR/review/review-comment activity, but not on a pure review-thread resolve/unresolve event, so GitHub's own required conversation resolution remains the durable blocker for that edge.
On 2026-04-20, issue #183 was executed as a clean-break rename from the previous product name to Wink. The repository now uses `Wink` / `com.wink.app` / `Application Support/Wink` as the canonical product identity, with no compatibility layer for legacy paths or bundle identifiers.
On 2026-04-20, issue #176 was re-integrated on top of current `main` instead of reviving stale PR #188 wholesale. Wink now supports Settings-based shortcut recipe export/import using a Wink-branded `.winkrecipe` JSON payload, preview-first conflict handling, bundle-id-first app resolution with app-name fallback, and unresolved-target warnings. The old PR's two real review gaps were folded into the current implementation: unresolved preview counts now exclude conflicting entries, and `ShortcutManager` now re-checks target-app availability during its existing permission poll so unavailable imported shortcuts stay persisted without being registered, then become active automatically after the app is installed (or are deregistered after removal) without another save or relaunch. This change is code/test verified in the local macOS Swift environment, and 2026-04-21 packaged-app UI spot-checks completed the live `NSOpenPanel` / `NSSavePanel` import/export flow against the current build.
macOS runtime validation under the new `Wink` identity now covers Accessibility/TCC recovery, standard-route packaged-app E2E, mixed standard+Hyper packaged-app E2E, Settings drag-to-reorder persistence, and the Settings import/export panel flow. Launch-at-login still remains pending because `com.wink.app` is a new login-item identity.
Wink was broadly validated on macOS 15.3.1 on 2026-03-20. On 2026-04-08, the shortcut-capture and toggle runtime were further refactored: standard shortcuts now use Carbon hotkeys, Hyper-dependent shortcuts remain on the active event tap, activation now defaults to front-process-only before escalating observation-driven window recovery, and toggle-off now uses `NSRunningApplication.hide()` with asynchronous confirmation. On 2026-04-08, targeted macOS re-validation was completed for the redesigned Safari/Hyper paths: Safari toggle-on/toggle-off now works again, Hyper-routed shortcuts survive fresh relaunches, and the post-fix runtime window shows `TOGGLE_HIDE_CONFIRMED` without new `TOGGLE_DEGRADED`, `hide_untracked`, event-tap-disable, or shortcut-capture resync-storm signatures. Broader app-matrix validation is still pending. A signed and notarized distributable is still unresolved.
On 2026-04-16, issue #138 realigned `docs/architecture.md` with the then-current codebase: stale `SettingsViewModel` references were removed, startup/add-shortcut/trigger flows described the actual `AppController -> ShortcutManager -> ShortcutCaptureCoordinator` and `SettingsWindowController -> ShortcutEditorState -> ShortcutManager.save()` pipelines at that time. That settings-window description was later superseded by issue #203's SwiftUI `Settings` scene migration, and the previous-app session wording was later superseded by issue #229's removal of that telemetry.
On 2026-04-16, issue #140 investigated the missing `System Settings > Privacy & Security > Input Monitoring` row that was seen during the packaged Hyper-only validation follow-up. The macOS 15.0 SDK headers for `CGPreflightListenEventAccess()` / `CGRequestListenEventAccess()` only promise effective event-listening access, not System Settings row visibility, and the original validation already showed the authoritative runtime signals (`Input Monitoring permission: granted`, active event tap, passing Hyper E2E). Wink now surfaces a non-blocking Settings banner note when Hyper capture is already active to clarify that the Input Monitoring pane can lag behind live access; troubleshooting guidance now treats `CGPreflightListenEventAccess()` plus event-tap health as the source of truth, while still calling out signature churn and launch-path mismatches as TCC confounders.
On 2026-04-16, GitHub workflow automation was added to close the issue/project drift exposed around #135: `.github/workflows/pr-metadata.yml` now blocks PRs that omit a closing keyword or a valid `Validation Status`, `.github/workflows/project-sync.yml` now reconciles the repository backlog project `Status` (`Ready` / `In Progress` / `Done`) plus `Runtime Validation` (`None` / `macOS pending` / `macOS complete`), linked issues are auto-added to the project if missing, and scheduled/manual reconciliation now backfills any repository issues that were never added because earlier events or API writes failed. This workflow requires a repository secret named `PROJECT_AUTOMATION_TOKEN` with `repo`, `project`, and `read:org` scope; see `docs/github-automation.md`.
On 2026-04-16, issue #133 made shortcut persistence strict and non-destructive again: `PersistenceService.load()` now fails loudly for malformed or unsupported `shortcuts.json`, logs the file path plus failure reason, and preserves a `shortcuts.load-failure-*.json` copy instead of silently collapsing runtime state to `[]`. A tag audit showed `isEnabled` landed before `v0.2.0` and the later `internal-downloads` prerelease, so the current policy intentionally does not migrate payloads missing `isEnabled`. The packaged `build/Wink.app` was then manually revalidated on macOS for malformed JSON, missing `isEnabled`, and unsupported-schema startup fixtures; in each case Wink stayed running, logged the failure plus preserved-copy path, left the original `shortcuts.json` untouched, and started with an empty in-memory shortcut set instead of silently overwriting persisted data.
On 2026-04-16, issue #135 tightened truthful standard-shortcut readiness around per-binding Carbon registration: `CarbonHotKeyProvider` now tracks desired vs. registered standard bindings plus failed keyCode/modifier/status tuples, `ShortcutCaptureCoordinator` only reports `carbonHotKeysRegistered` / `standardShortcutsReady` when every enabled standard binding registered successfully, `SHORTCUT_TRACE_BLOCKED reason="missing_registration_or_system_conflict"` now includes failed-binding details, and the settings banner now warns instead of claiming standard shortcuts are active during partial registration failure. Packaged-app macOS runtime validation is now complete for the current standard-only fixture via `bash scripts/e2e-full-test.sh`; a live Carbon partial-registration reproduction on this machine is still pending because neither common system-reserved shortcuts nor a second hotkey-holder helper process induced `RegisterEventHotKey` failure here.
On 2026-04-16, issue #136 refreshed the Settings shortcut-readiness banner on app reactivation: `SettingsViewLifecycleHandler.handleAppDidBecomeActive()` now refreshes `shortcutCaptureStatus` in addition to launch-at-login state, and the regression is covered by `handleAppDidBecomeActiveRefreshesShortcutCaptureAndLaunchAtLoginStatus()`. Packaged-app runtime validation for the current build was re-run on macOS 15.3.1 with both the user's standard-only Safari fixture and a temporary mixed fixture (`Safari` standard + `IINA` Hyper), producing passing `bash scripts/e2e-full-test.sh` runs in both modes (`6/6 PASS, 1 warning` on the standard-only fixture because Hyper was a configuration skip; `6/6 PASS, 0 warnings` on the temporary mixed fixture). The destructive permission-flip path is now also complete: after `tccutil reset All com.wink.app`, packaged `build/Wink.app` reopened with the Settings banner showing `Accessibility permission required`; after re-granting permissions in System Settings and returning to the same Wink Settings window without clicking `Refresh`, the banner automatically changed to `Shortcut capture ready` / `Standard and Hyper shortcuts are active.`
On 2026-04-16, issue #137 fixed the remaining launch-at-login bool-collapse in the then-AppKit menu bar: `LaunchAtLoginSnapshot` stopped being inverted into a simple bool, `.requiresApproval` became an approval CTA into Login Items settings, and `.notFound` started distinguishing install-location guidance from real configuration failure. The menu-bar-specific AppKit tests from that change were later retired by issue #204 when the SwiftUI `MenuBarExtra` shell replaced `MenuBarController`, but the presentation logic itself continues to live through `LaunchAtLoginPresentation` in General settings.
On 2026-04-16, the test suite's persistence isolation was hardened so `swift test` no longer touches the real `~/Library/Application Support/Wink/shortcuts.json`. A shared `Tests/WinkTests/TestSupport/TestPersistenceHarness.swift` now vends temporary-directory-backed `PersistenceService` instances, the `ShortcutManager` / `AppPreferences` / `SettingsViewLifecycleHandler` / `ShortcutEditorState` test helpers now use that harness instead of live `PersistenceService()` defaults, and a regression test (`helperBuiltShortcutManagerCanPersistIntoInjectedHarness`) now guards the injected-storage path explicitly. This was verified by a passing full `swift test` run and identical pre/post `shasum` values for the real user shortcut payload.
On 2026-04-17, the packaged-app E2E harness stopped assuming the old fixed local fixture (`Safari` standard plus `IINA` Hyper). `scripts/e2e-lib.sh` now derives a supported shortcut inventory directly from the current enabled `shortcuts.json` entries, the app-level modules (`Configured Toggle ON/OFF`, `Debounce & Cooldown`, `Toggle Loop Prevention`, `Multi-Shortcut Isolation`) now select their targets dynamically from that inventory, and general Hyper-route sends now use the more reliable `osascript` compatibility path while the dedicated Hyper hold module still keeps its explicit `CGEvent` probe plus fallback behavior. This prevents Hyper-only or otherwise drifted local fixtures from surfacing false product failures while still preserving route-specific readiness checks. On the current Hyper-only local fixture (`Antigravity`, `Chrome`, etc.), packaged `build/Wink.app` now re-validates with `bats scripts/e2e-lib.bats` passing and `bash scripts/e2e-full-test.sh` reporting `All 6 tests passed (0 warnings)`.
On 2026-04-15, issue #134 narrowed Input Monitoring prompting to actual Hyper transport demand: startup now inspects the saved shortcut set plus persisted Hyper state before deciding whether to call `CGRequestListenEventAccess()`, runtime route changes (enabling Hyper, or adding/enabling Hyper-routed shortcuts) now trigger the prompt and event-tap resync only when the configuration crosses from standard-only into Hyper-required, and Hyper-required startup now defers the Input Monitoring request until Accessibility has actually been granted so the follow-up AX poll can re-prompt Input Monitoring reliably. macOS runtime validation for the new permission-prompt timing is now complete for the standard-only and Hyper-only paths below.
On 2026-04-09, toggle reliability recovery moved lifecycle ownership fully into `ToggleSessionCoordinator`: launch / activate / stable / deactivation state is now pid-aware, attempt-scoped, and no longer split between coordinator and `AppSwitcher`. Regular apps now require usable window evidence (`visibleWindowCount > 0` or focused/main-window evidence) before toggle-on can be recorded as stable success; only non-regular apps may succeed windowlessly. The same recovery also added attempt-linked `TOGGLE_TRACE_*` and accepted-trigger `SHORTCUT_TRACE_*` diagnostics so one log window can explain branch choice, reset reason, and confirmation outcome end-to-end.
On 2026-04-08, launch-at-login presentation was hardened so `SMAppService.Status.notFound` is no longer always treated as a packaging failure: when Wink is running outside `/Applications` or `~/Applications`, the General tab now shows install-location guidance instead of the red bundle-misconfiguration warning.
On 2026-04-08, local DMG packaging and a tag-driven GitHub release workflow were added. The repo can now build `build/Wink-<version>.dmg` locally and defines the credential-backed notarization/publish path. On 2026-04-08, the release workflow was further hardened to preflight required signing/notarization secrets and skip cleanly with a summary when Developer ID credentials are absent, and a separate internal-package workflow was added to upload unsigned DMG artifacts for trusted testers. The internal workflow now also maintains a stable `internal-downloads` tag-backed prerelease page with tester-facing install notes and the latest internal DMG asset, instead of deleting and recreating the release each run.
On 2026-04-09, packaged-app runtime validation moved past the original TCC blocker after `build/Wink.app` was re-added to Accessibility and Input Monitoring. Manual Safari validation on the packaged app now shows the owned launch / relaunch path attaching the launched process back to the same attempt (`TOGGLE_TRACE_SESSION ... event=launch_attached reason="launch_completion_process_lookup"`), frontmost-without-window states staying in visibility recovery (`TOGGLE_TRACE_CONFIRMATION ... event=awaiting_window_evidence`) until usable window evidence appears, and second press hiding cleanly via `TOGGLE_HIDE_CONFIRMED` instead of falling through to `hide_untracked`. A follow-up investigation also closed the earlier `carbon=false eventTap=true` mystery after restart: at that point Safari itself was Hyper-routed, so the event-tap path was the correct transport. The current local fixture has since moved to a mixed transport setup (`command + shift + s` for Safari plus a Hyper-routed IINA shortcut), and the latest packaged-app verification now shows `checkPermission: ax=true im=true carbon=true eventTap=true` together with clean `TOGGLE_STABLE` / `TOGGLE_HIDE_CONFIRMED` chains and no fresh degraded/session-reset signatures in the recent log window.

## Automated Verification

### 2026-08-13
- issue #465 (post-merge hardening of #460/#461) implemented and verified: `swift test` passed (1033 tests) and `swift build -c release` passed
- three parked findings closed with regression tests: diagnostics-redaction timestamp monotonicity (+ clock-rollback baseline reset and writer-side queue-order timestamp sampling), the B→A snapshot-adoption condition (canonical mirror AND data file, absent-file repair, mirror-last-copy guard), and the stale unreadable-recovery banner on an armed successor
- locally reviewed with `codex review` (gpt-5.6-sol) before push; the GitHub Codex bot passed the current head with 👍 after two P2 findings were fixed and their threads resolved
- the forward-timestamp continuation residual in `DiagnosticsRedactor` is explicitly out of scope (adversarial-only; the "owns the machine" threat model already concedes it)

### 2026-04-20
- `swift test --filter 'WinkRecipeCodec|WinkRecipeImportPlanner|ShortcutEditorStateTests|ShortcutManagerStatusTests|AppListProviderTests'` passed; the focused recipe/import/availability suite reported 40 tests passed, including the new issue #176 regression coverage
- after removing stale `Wink` TCC rows and re-adding the exact current `build/Wink.app`, a fresh packaged relaunch on the current standard-only `Safari` / `Terminal` / `Notes` fixture logged `start(): ready=true, ax=true, im=true, inputMonitoringRequired=false` plus `attemptStart: shortcuts=3 triggerIndex=3 carbon=true eventTap=false`
- `bash scripts/e2e-full-test.sh` passed on packaged `build/Wink.app` with `All 6 tests passed (1 warnings)`; the single warning remained the expected Hyper-module skip on that standard-only fixture
- a manual Settings drag-to-reorder validation temporarily forced the packaged app to open Settings, reordered the visible `Safari` / `Terminal` / `Notes` rows to `Terminal -> Notes -> Safari`, and observed the same persisted order in `~/Library/Application Support/Wink/shortcuts.json`
- after removing the temporary validation hook, restoring the original shortcut order, rebuilding clean, and re-granting TCC for the new ad-hoc signature, `bash scripts/e2e-full-test.sh` passed again with `All 6 tests passed (1 warnings)`

### 2026-04-21
- `swift test --filter 'LayoutRegressionTests|InsightsViewModelTests'` passed after issue #181; the focused Insights regression suite reported 6 tests passed, covering the new natural-language summary copy and existing refresh/window semantics
- `swift test --filter 'ShortcutManagerStatusTests|ShortcutEditorStateTests'` passed; the focused post-review regression suite reported 29 tests passed
- `swift test` passed; the full suite reported 256 tests passed after the issue #181 Insights layout/presentation additions
- `swift build` passed
- `git diff --check` passed with no whitespace or merge-marker issues
- `bash -n scripts/package-app.sh scripts/package-update-zip.sh scripts/generate-appcast.sh scripts/export-test-sparkle-keys.sh` passed
- `.github/workflows/ci.yml` and `.github/workflows/release.yml` both parsed successfully via Ruby `YAML.load_file`
- `python3 -m py_compile scripts/upload-r2-object.py` passed
- `bash scripts/package-app.sh` passed and produced `build/Wink.app` with embedded `Sparkle.framework`; `codesign --verify --deep --strict --verbose=2 build/Wink.app` also passed, and `build/Wink.app/Contents/Frameworks/Sparkle.framework/Versions/B/XPCServices` was absent as intended for this unsandboxed app
- `bash scripts/package-update-zip.sh` passed and produced `build/Wink-0.3.0.zip`
- `R2_ACCOUNT_ID=test-account R2_ACCESS_KEY_ID=test-access-key R2_SECRET_ACCESS_KEY=test-secret-key R2_BUCKET=test-bucket R2_PUBLIC_BASE_URL='https://example.invalid/wink/' python3 scripts/upload-r2-object.py build/Wink-0.3.0.zip application/zip --dry-run` passed
- `SPARKLE_PUBLIC_BASE_URL='https://example.com/wink/' bash scripts/generate-appcast.sh` reached Sparkle's signing step but failed as expected on the local machine because no private EdDSA key was available; signed appcast generation remains blocked on real Sparkle signing credentials
- packaged `build/Wink.app` Settings validation exercised the live `Export...` / `Import...` flow: exporting wrote `~/Library/Application Support/Wink/Wink.winkrecipe`, re-importing that file showed `0 ready, 3 conflicts, 0 unresolved`, and applying `Skip Conflicts` left `~/Library/Application Support/Wink/shortcuts.json` unchanged while the UI reported `Imported 0 shortcuts`
- after removing and re-adding the exact current `build/Wink.app` in both Accessibility and Input Monitoring, `bash scripts/e2e-full-test.sh` passed on the restored standard-only `Safari` / `Terminal` / `Notes` fixture with `All 6 tests passed (1 warnings)`; the single warning was the expected Hyper-module configuration skip
- after temporarily swapping `~/Library/Application Support/Wink/shortcuts.json` to a mixed fixture (`Safari` standard + `Terminal` Hyper), enabling `hyperKeyEnabled`, and restoring the original user fixture afterward, packaged `build/Wink.app` passed `bash scripts/e2e-full-test.sh` again with `All 6 tests passed (0 warnings)`
- the same mixed-fixture packaged-app log window showed `checkPermission: ax=true im=true carbon=true eventTap=true`, `EVENT_TAP_SWALLOW: ... hyperInjected=true`, `HYPER_INJECT: ... matched=true`, `SHORTCUT_TRACE_DECISION ... route=hyper`, and clean `TOGGLE_STABLE` / `TOGGLE_HIDE_CONFIRMED` sequences, confirming that Carbon and event-tap capture were both active on the same current build

### 2026-04-09
- `swift test` passed; the suite reported 187 tests passed
- `swift test --filter AppSwitcherTests` passed
- `swift test --filter AppControllerTests` passed
- `swift test --filter ShortcutManagerStatusTests` passed
- `swift test --filter ToggleSessionCoordinatorTests` passed
- `swift test --filter ToggleLaunchLifecycleTests` passed
- `bats scripts/e2e-lib.bats` passed
- `bash scripts/package-app.sh` passed, rebuilt `build/Wink.app`, and re-signed it with the local Wink identity
- `codesign --verify --deep --strict --verbose=2 build/Wink.app` passed
- `bash scripts/e2e-full-test.sh` failed at packaged-app runtime preflight because TCC was not granted for `build/Wink.app` in this environment (`trusted=false`, `ax=false`, `im=false`, event tap never started)
- a follow-up packaged-app spot-check validated standard external activation against the current `command + shift + s` Safari shortcut: `MATCHED`, `TOGGLE_TRACE_DECISION event=hide_untracked`, `TOGGLE_HIDE_UNTRACKED`, `HIDE_REQUEST`, and `TOGGLE_HIDE_CONFIRMED` all appeared with the same non-nil `attemptId` / `pid` / `phase`
- after the E2E harness was updated to use transport-aware readiness plus config-aware module skips, `bash scripts/e2e-full-test.sh` passed on the local fixture
- with the current mixed saved fixture (`Safari` standard + `IINA` Hyper), `bash scripts/e2e-full-test.sh` passed with `6/6 PASS` and `0 warnings`

### 2026-04-15
- `swift test --filter ShortcutManagerStatusTests` passed; the suite reported 15 tests passed, including the new startup deferral regression case
- `swift test` passed; the suite reported 199 tests passed
- `bash scripts/package-app.sh` passed, rebuilt `build/Wink.app`, and re-signed it with the local Wink identity
- After `tccutil reset All com.wink.app`, packaged `build/Wink.app` cold-started under the Hyper-only IINA fixture with `start(): ready=false, ax=false, im=false, inputMonitoringRequired=true`
- After enabling Accessibility later on that same packaged build, the follow-up permission poll logged `Accessibility permission: granted`, `Input Monitoring permission: granted`, `Event tap started (background thread)`, and `attemptStart: shortcuts=1 triggerIndex=1 carbon=false eventTap=true`
- `bash scripts/e2e-test-hyper-key.sh` passed on the packaged build after that AX-grant recovery path (`7 passed`)
- After restoring the local standard-only Safari fixture, `bash scripts/e2e-full-test.sh` passed on the packaged build with `All 6 tests passed (1 warnings)`; the Hyper module warning was an expected fixture skip because the temporary IINA Hyper shortcut had been removed

### 2026-04-16
- `swift test --filter ShortcutManagerStatusTests` passed; the suite reported 16 tests passed, including the new partial-Carbon-registration regression coverage for issue #135
- `swift test --filter handleAppDidBecomeActiveRefreshesShortcutCaptureAndLaunchAtLoginStatus` failed first, then passed after the issue #136 lifecycle fix was applied
- `swift test --filter LaunchAtLogin` passed; the suite reported 18 launch-at-login focused tests passed, including the new menu-bar title/availability coverage for issue #137
- `swift test --filter SettingsView` passed
- `swift test --filter AppPreferences` passed
- `swift test` passed; the suite reported 208 tests passed
- `bash scripts/package-app.sh` passed and rebuilt `build/Wink.app` after the issue #137 menu-bar change
- `bash scripts/e2e-full-test.sh` passed on packaged `build/Wink.app` with the current standard-only Safari fixture (`6/6 PASS`, `1 warning` because the Hyper module was a configuration skip)
- after temporarily swapping `~/Library/Application Support/Wink/shortcuts.json` to a mixed fixture (`Safari` standard + `IINA` Hyper) and restoring the user's original file afterward, `bash scripts/e2e-full-test.sh` passed again on packaged `build/Wink.app` with `6/6 PASS` and `0 warnings`
- after temporarily swapping `~/Library/Application Support/Wink/shortcuts.json` to the same mixed fixture, `tccutil reset All com.wink.app` produced a packaged-app cold start with the Settings banner showing `Accessibility permission required` / `Accessibility permission is required for app switching.`
- after re-granting permissions in System Settings and clicking back into the same packaged Wink Settings window without using the banner's `Refresh` button, AX inspection of the live SwiftUI window reported `Shortcut capture ready` / `Standard and Hyper shortcuts are active.`
- the same permission-flip run logged `Accessibility permission: granted`, `Input Monitoring permission: granted`, `Accessibility ready — syncing shortcut capture`, `Event tap started (background thread)`, and `attemptStart: shortcuts=2 triggerIndex=2 carbon=true eventTap=true`
- A targeted issue #135 runtime probe temporarily rewrote `shortcuts.json` to exercise partial standard-registration failure. Common system-reserved shortcuts (`command-space`, `control-space`, screenshot shortcuts, `command-tab`) and a separate helper process holding `command-shift-t` all still let Wink report `carbon=true` on this machine, so a live Carbon conflict reproduction remains pending even though the packaged-app E2E suite and code-level regression coverage both passed.
- Packaged-app manual runtime validation for issue #133 passed with three startup fixtures written directly to `~/Library/Application Support/Wink/shortcuts.json` one at a time, restoring the user's original fixture afterward:
  - malformed JSON payload
  - payload missing `isEnabled`
  - unsupported wrapper schema (`{"schemaVersion":2,"shortcuts":[]}`)
- In all three cases Wink logged `Failed to load shortcuts: path=... reason=... preservedCopyPath=...`, logged `Startup skipped shortcut restore because persistence loading failed`, kept running after launch, and reported `attemptStart: shortcuts=0 triggerIndex=0 carbon=false eventTap=false`
- In all three cases Wink created exactly one `shortcuts.load-failure-*.json` copy, that copy matched the unreadable source payload byte-for-byte, and the original `shortcuts.json` remained unchanged both while the app was running and after it exited
- `swift test --filter PersistenceServiceDiskLoadingTests` passed; the suite reported 5 tests passed, including the new shared temporary-storage harness coverage
- `swift test --filter helperBuiltShortcutManagerCanPersistIntoInjectedHarness` passed
- `swift test` passed; the suite reported 214 tests passed
- `shasum ~/Library/Application\ Support/Wink/shortcuts.json` was identical before and after `swift test`, confirming the test suite no longer mutates the live user shortcut fixture

### 2026-04-17
- `bats scripts/e2e-lib.bats` passed after adding regression coverage for dynamic E2E target selection (`resolve_primary_test_shortcut`, Hyper fallback, and multi-shortcut isolation selection)
- `bash scripts/e2e-test-debounce.sh` initially failed on the Hyper-only fixture because the new generic Hyper sender was using the brittle `CGEvent` path for rapid-repeat tests; after routing the generic Hyper helpers through the `osascript` compatibility path and keeping the dedicated Hyper hold module on its explicit probe-driven sender, the standalone debounce module passed (`4 passed`)
- `bash scripts/e2e-full-test.sh` passed on packaged `build/Wink.app` with the current real Hyper-only fixture (`All 6 tests passed`, `0 warnings`)
- the same packaged-app run started with `start(): ready=true, ax=true, im=true, inputMonitoringRequired=true`, `attemptStart: shortcuts=20 triggerIndex=20 carbon=false eventTap=true`, matching the current Hyper-only saved shortcut set instead of the older mixed-fixture baseline

### 2026-04-08
- `swift test` passed after the DMG packaging and release workflow changes; the suite reported 173 tests passed
- `swift build` passed after the refactor
- `./scripts/package-app.sh` passed after the refactor, rebuilt `build/Wink.app`, and re-signed it with the local Wink identity
- `./scripts/package-dmg.sh` passed and produced `build/Wink-0.2.0.dmg`
- Workflow regression checks passed for release preflight gating, manual `release_tag` dispatch, and internal-package artifact upload wiring

## Validated on macOS
- Broad real-device validation completed on macOS 15.3.1 on 2026-03-20
- `swift build`, `swift test`, release build, and `./scripts/package-app.sh` passed
- Dual permission gating, active capture startup, and end-to-end shortcut interception were validated
- Runtime toggle behavior, direct-hide confirmation, and window recovery paths were exercised successfully
- Insights persistence and restart behavior were confirmed during the macOS pass
- On 2026-04-08, targeted post-redesign validation confirmed Safari toggle-on/toggle-off behavior again on a real macOS session
- On 2026-04-08, Hyper-routed shortcuts were re-validated after a fresh relaunch; the startup-state replay fix restored live `HYPER_INJECT` / `EVENT_TAP_SWALLOW` behavior
- The post-fix runtime window showed repeated `HIDE_REQUEST` -> `TOGGLE_HIDE_CONFIRMED` pairs without fresh `TOGGLE_DEGRADED`, `TOGGLE_HIDE_DEGRADED`, `hide_untracked`, event-tap-disable diagnostics, or repeated "syncing shortcut capture" churn
- Broader 2026-04-08 app-matrix validation for system/window-weird apps remains pending
- On 2026-04-09, packaged `build/Wink.app` was manually revalidated for Safari fresh-launch and relaunch after TCC was granted:
  - fresh launch produced `event=session_started activationPath=launch`, `event=launch_attached reason="launch_completion_process_lookup"`, `event=awaiting_window_evidence`, then `event=confirmed reason="activation_stable"`
  - the next press produced `TOGGLE_HIDE_ATTEMPT`, `HIDE_REQUEST`, and `TOGGLE_HIDE_CONFIRMED` with Finder frontmost afterward
  - relaunch after termination produced `TOGGLE_TRACE_RESET ... reason="termination"`, then a new owned `launch` attempt with a new pid, followed by the same stable-confirmed -> hide-confirmed sequence
- On 2026-04-09, packaged `build/Wink.app` was also manually revalidated for Safari external activation under the current Hyper configuration:
  - externally fronting Safari, then triggering the persisted Hyper shortcut, produced `TOGGLE_TRACE_DECISION event=hide_untracked reason="external_untracked_hide"`, followed by a real `HIDE_REQUEST` and `TOGGLE_HIDE_CONFIRMED`
  - the current packaged-app configuration is Hyper-routed, so `checkPermission: ax=true im=true carbon=false eventTap=true` is expected in that state, not a Carbon regression
- On 2026-04-09, packaged `build/Wink.app` was then manually revalidated again for Safari external activation after switching the persisted shortcut back to the standard `command + shift + s` route:
  - externally fronting Safari, then triggering the standard shortcut, produced `MATCHED`, `TOGGLE_TRACE_DECISION event=hide_untracked`, `TOGGLE_HIDE_UNTRACKED`, `HIDE_REQUEST`, and `TOGGLE_HIDE_CONFIRMED`
  - the very first `hide_untracked` trace and lifecycle lines now carried the same non-nil `attemptId`, `pid`, and `phase=deactivating` as the later hide request / confirmation lines, confirming that the coordinator-owned deactivation session is allocated before logging that branch
- On 2026-04-09, the frontmost/no-window policy was also observed live on Safari: Wink logged `awaiting_window_evidence` and recovery stages while Safari was frontmost without usable window evidence, and only promoted to stable once visible/focused/main-window evidence appeared
- On 2026-04-09, a later mixed-transport validation pass also exercised Safari standard toggles and IINA Hyper toggles in the same packaged session:
  - the saved shortcut fixture contained both Safari standard and IINA Hyper bindings, so `checkPermission: ax=true im=true carbon=true eventTap=true` was the expected steady-state readiness snapshot
  - the recent runtime log window showed repeated `TOGGLE_STABLE` -> `TOGGLE_HIDE_CONFIRMED` pairs for both apps, plus expected cooldown blocks under rapid repeat input, but no fresh `TOGGLE_DEGRADED`, `TOGGLE_HIDE_DEGRADED`, `phase=no_session`, `hide_untracked`, or capture-blocked signatures
- On 2026-04-15, issue #134's permission-scope change was revalidated on packaged `build/Wink.app` in both transport modes:
  - with the restored standard-only Safari fixture, Wink was previously revalidated under `tccutil reset All` followed by granting only Accessibility, producing `checkPermission: ax=true im=false carbon=true eventTap=false` and a passing `bash scripts/e2e-full-test.sh` run with the Hyper module skipped as configuration-only
  - with a temporary Hyper-only IINA fixture, a fresh `tccutil reset All` cold start produced `start(): ready=false, ax=false, im=false, inputMonitoringRequired=true`; granting Accessibility later in the same session then produced `Input Monitoring permission: granted`, `Event tap started (background thread)`, and a passing `bash scripts/e2e-test-hyper-key.sh`
- On 2026-04-16, issue #133's persistence failure handling was manually revalidated on packaged `build/Wink.app`:
  - malformed JSON, missing-`isEnabled`, and unsupported-schema fixtures all produced a startup-time `Failed to load shortcuts` log with `path`, `reason`, and `preservedCopyPath`
  - Wink kept running after each failed load and started with `attemptStart: shortcuts=0 triggerIndex=0 carbon=false eventTap=false`, confirming that unreadable persistence no longer materializes as an in-memory shortcut set
  - each failed load preserved a byte-identical `shortcuts.load-failure-*.json` copy and left the original `shortcuts.json` untouched

## Toggle Loop Fix and Cross-App Restore (2026-03-25, Issue #80)
- **Toggle loop root cause**: physical key repeat events spaced > 200ms bypassed debounce, causing activate/hide/activate cycles
- **Three-layer defense**: debounce 200ms + per-bundle toggle cooldown 400ms + re-entry guard (reduced from 500ms/800ms in issue #82; Layer 1 autorepeat filter is the primary defense)
- **Cross-app restore bug found and fixed**: when App A's toggle-off restores App B, pressing App B's shortcut failed to toggle it off. Root cause: `stableActivationState` for B was cleared when A was toggled on, and never recreated when B was restored externally. Fixed by adding `ACTIVE_UNTRACKED` path that hides the app when it is frontmost but has no tracking state.
- **Additional hardening**: coordinator invalidation logging, lastToggleTimeByBundle eviction, activate-path isActive warning

## Capture / Activation / Hide Refactor (2026-04-08)
- Standard shortcuts now register through Carbon `EventHotKey`; the active event tap remains only for Hyper-routed shortcuts.
- Input Monitoring no longer blocks standard-shortcut readiness when the current shortcut set does not require the Hyper path.
- Stable activation now defaults to front-process activation only. `makeKeyWindow`, `AXRaise`, and reopen/new-window recovery run only if observation shows the activation is not yet settled.
- Stable toggle-off no longer restores the previous app first for normal app-level hides.
- Toggle-off now uses the official `NSRunningApplication.hide()` request, then keeps the session in `deactivating` until hide is actually confirmed.
- Deactivation confirmation is notification-first: `NSWorkspace.didHideApplicationNotification` completes the session immediately when available, with a short observation window as a bounded backstop.
- A target is not considered toggled off merely because another app became frontmost; confirmation requires the target to be not frontmost and either independently hidden or successfully observed as windowless. A failed AX windows read is unknown, not zero-window evidence. This closes both the Safari-visible-behind-Finder gap and the unknown-observation false confirmation.
- Repeat presses while the target is still deactivating no longer clear stable state or reactivate the target.

## Implemented Behavioral Guarantees
- Half-active frontmost mismatches are not promoted to stable: `ActivationObservationSnapshot.isStableActivation` requires target/frontmost agreement, `targetIsActive == true`, `targetIsHidden == false`, and supporting window evidence before `AppSwitcher` can promote a pending activation
- A second press during pending activation does not toggle off: `pendingActivationState` blocks `shouldToggleOff`; after recovery exhaustion, a repeat atomically re-confirms the same attempt and generation instead of restoring away or allocating a fresh recovery budget
- Confirmation failure does not trigger flicker-inducing restore-away rollback: exhausted recovery enters generation-owned `degraded`; accepted repeats return to `activating`, while retry-cap or absolute-ceiling termination enters `idle` before any further activation, hide, or recovery side effect
- Stable toggle-off now enters an explicit `deactivating` phase, requests `hide()` on the target app, and clears runtime session state only after hide confirmation succeeds
- Event tap recovery now either recreates the tap successfully on the existing background RunLoop thread or leaves an explicit degraded readiness state after repeated recreation failures
- Standard shortcuts and Hyper shortcuts now report readiness independently, so missing Input Monitoring only degrades Hyper capture instead of the whole app
- `ToggleSessionCoordinator` is now the canonical toggle owner: launch / activate / stable / deactivating state and pid rollover handling no longer depend on split local `AppSwitcher` state
- Relaunches now allocate an owned `launching` session before `NSWorkspace` open returns, so the next press cannot fall through to `hide_untracked` merely because the process was between lifetimes
- `NSWorkspace.openApplication` completion is now used as a process-identity seam: the returned `NSRunningApplication` feeds pid attachment and the same confirmation pipeline used by activate/unhide, instead of leaving launch as fire-and-forget
- Regular apps cannot silently succeed toggle-on without usable window evidence; only targets with `activationPolicy != .regular` may remain stable without visible/focused/main-window proof
- `hide_untracked` now creates an explicit coordinator-owned `deactivating` session before dispatching `hide()`, so external activation still gets a real `HIDE_REQUEST` / confirmation pair instead of only logging the branch
- Activation recovery exhaustion is now wired into the production coordinator flow: degraded repeats retain one `attemptID`/generation, consume at most the configured retry budget, and cannot execute another AX raise, reopen, Command-N, hide, or activation action after the retry cap or absolute ceiling terminates the session

## Toggle Reliability Recovery (2026-04-09)
- `ToggleSessionCoordinator` now owns pid-aware attempt sessions with explicit `launching`, `activating`, `activeStable`, `deactivating`, `degraded`, and `idle` phases.
- `AppSwitcher` derives pending/stable views from the coordinator instead of keeping its own mutable lifecycle owner.
- `ToggleDiagnosticEvent` centralizes `TOGGLE_TRACE_*` formatting so attempt-linked branch logs stay cheap and consistent.
- `ShortcutManager` emits `SHORTCUT_TRACE_*` only for matched shortcuts or explicit blocked-capture states, not for unrelated key events.

### Trace signatures to treat as success
- Toggle-on settled: `TOGGLE_TRACE_CONFIRMATION attemptId=... event=confirmed reason="activation_stable"`
- Toggle-off settled: `TOGGLE_TRACE_CONFIRMATION attemptId=... event=confirmed reason="hide_confirmed"`
- Owned launch path started correctly: `TOGGLE_TRACE_SESSION attemptId=... event=session_started activationPath=launch reason="not_running_launch_request"`
- Owned launch attached to the real process: `TOGGLE_TRACE_SESSION attemptId=... event=launch_attached activationPath=launch reason="launch_completion_process_lookup"`
- Activation recovery exhausted under the owned attempt: `TOGGLE_TRACE_CONFIRMATION attemptId=... event=degraded reason="activation_recovery_exhausted"`
- Degraded repeat decision: `TOGGLE_TRACE_DECISION attemptId=... event=degraded_reconfirm reason="result=accepted|retry_capped|absolute_ceiling_reached ..."`
- Hyper-routed IINA in the current local config: `SHORTCUT_TRACE_DECISION event=matched bundle=com.colliderli.iina route=hyper`

### Trace signatures to treat as failure or follow-up
- Regular app frontmost but still unusable: `TOGGLE_TRACE_CONFIRMATION attemptId=... event=awaiting_window_evidence reason="frontmost_without_window_evidence"`
- Hide request degraded instead of settling: `TOGGLE_TRACE_CONFIRMATION attemptId=... event=degraded reason="partial_hide_degraded"`
- Degraded target termination: `TOGGLE_TRACE_RESET attemptId=... event=session_idled reason="termination"`
- Session reset outside degraded recovery: `TOGGLE_TRACE_RESET attemptId=... event=session_cleared reason="termination" | "pid_rollover" | "launch_failed"`
- Stale tracking corrected: `TOGGLE_TRACE_RESET attemptId=... event=session_invalidated reason="stale_state_invalidated"`
- Capture path blocked before toggle dispatch: `SHORTCUT_TRACE_BLOCKED reason="missing_registration_or_system_conflict" | "input_monitoring_missing" | "event_tap_inactive"`

### Trace signatures that are only valid in specific ownership cases
- `TOGGLE_TRACE_DECISION event=hide_untracked reason="external_untracked_hide"` is acceptable only for genuinely external activation. It is a regression if it appears immediately after an owned launch or relaunch.
- When `hide_untracked` is valid, it must still be followed by a real hide lane (`HIDE_REQUEST` and `TOGGLE_HIDE_CONFIRMED` or explicit degraded confirmation). A lone `hide_untracked` log without a deactivation session is a bug.
- `TOGGLE_TRACE_DECISION event=blocked reason="activation_pending_not_stable"` is the correct second-press behavior while an owned launch/activation is still settling.
- `checkPermission: ax=true im=true carbon=false eventTap=true` is only suspicious if the current enabled shortcut set is supposed to be standard-only. If the persisted shortcut is Hyper-routed and `hyperKeyEnabled` is on, that snapshot is expected.
- `checkPermission: ax=true im=true carbon=true eventTap=true` is the expected steady-state snapshot when the saved fixture mixes standard and Hyper shortcuts.
- `scripts/e2e-full-test.sh` and `scripts/e2e-lib.sh` now preflight packaged-app startup with transport-aware readiness: standard-only configs accept `carbon=true eventTap=false`, Hyper-only configs require an active event tap, and mixed fixtures require both transports to be ready before modules start.
- E2E modules should treat missing fixture shortcuts as configuration skips, not runtime failures. The harness now resolves its test targets from the current enabled shortcut set instead of assuming Safari/IINA, so warning-free full-suite results no longer depend on a hard-coded local fixture as long as the saved config exposes enough supported shortcuts for the module being exercised.

## Follow-up Requiring macOS Validation
- If you want to validate one transport in isolation, trim the saved fixture to that transport first and read the actual `shortcuts.json` before interpreting readiness logs. Do not assume the local machine is still on the older mixed Safari/IINA fixture.
- Fresh-launch path
  Validate: target not running -> shortcut launches it -> second press hides it without `hide_untracked` or `phase=no_session`
- Relaunch path
  Validate: target stabilizes -> target quits -> shortcut relaunches it -> second press still hides cleanly
- External activation path
  Validate: user fronts the target outside Wink -> Wink only uses `hide_untracked` when the session is truly unowned, and that branch still emits a real `HIDE_REQUEST` / `TOGGLE_HIDE_CONFIRMED` pair
- Frontmost/no-window path
  Validate: target becomes frontmost without visible/focused/main-window evidence -> Wink stays in visibility recovery or degraded state instead of recording stable success
- Hotkey-latency regression
  Validate: repeated matched shortcuts do not show perceptible latency after the new attempt/session diagnostics
- Normal apps beyond Safari: Finder, Terminal
  Validate stable activation, second-press toggle-off via `hide()`, and coherent `TOGGLE_STABLE` / `TOGGLE_HIDE_CONFIRMED` diagnostics
- System or window-weird apps: Home, Clock, System Settings
  Validate that visible-but-not-frontmost states do not promote to stable and that repeat presses during pending/degraded activation keep re-confirming instead of toggling away
- Hidden app reactivation
  Validate that an already-hidden target can return to `activeStable` and still hide cleanly on the next confirmed toggle-off
- Minimized window recovery
  Validate that `makeKeyWindow` / `AXRaise` / reopen recovery stages settle before confirmation promotion and that missing focused/main-window evidence prevents false stable promotion
- Fast repeated same-shortcut presses
  Validate debounce plus pending-session behavior under rapid repeated input for the same target
- Event tap timeout/recovery stress
  If reproducible on macOS, trigger timeout churn and confirm: first timeout re-enables in place, 3 timeouts within 30 seconds escalate to recreation, and repeated recreation failures surface degraded readiness/logs
- Shortcut transport split
  Validate that standard shortcuts keep working without Input Monitoring, while Hyper shortcuts correctly require it and recover when the permission is granted later
- Standard Carbon partial-registration conflict path
  Validate that when one standard binding is blocked by an actual Carbon/system shortcut conflict, Wink reports `carbon=false`, keeps `standardShortcutsReady=false`, surfaces the banner warning, and logs the failed keyCode/modifier tuple in `SHORTCUT_TRACE_BLOCKED`
- Launch-at-login approval flow after the 2026-03-23 issue #67 approval-state UX update and the 2026-04-16 issue #137 menu-bar fix, especially `.requiresApproval` -> `.enabled` foreground refresh, the menu-bar Login Items CTA path, and `.notFound` behavior on real installs
- Launch-at-login validation should use a packaged app installed in `/Applications` or `~/Applications`; repo-local `build/Wink.app` runs now surface install-location guidance instead of masquerading as a broken bundle
- Active event-tap startup and readiness reporting after permission or lifecycle changes
- AppSwitcher fallback behavior after SkyLight failure now that it re-requests activation via `NSWorkspace`
- Hyper Key failure handling, especially persistence only after `hidutil` succeeds
- Insights date-window and refresh-race fixes
- Signed/notarized distributable workflow once a Developer ID certificate is available
- Credential-backed validation of `.github/workflows/release.yml`, including certificate import, notarization, stapling, `spctl`, and GitHub Release upload
- Internal-package workflow download/install validation on a clean macOS machine

## Operational Caveats
- Standard shortcuts require Accessibility plus successful Carbon registration; Hyper shortcuts additionally require Input Monitoring plus a successfully started active event tap
- Ad-hoc signing changes can invalidate TCC state; use `tccutil reset` during development when needed
- Launch the app with `open`, not by executing the binary directly, so TCC matches the correct app identity
- SkyLight is a private API dependency for reliable activation from LSUIElement apps and may block App Store submission
- If SkyLight activation fails, Wink now falls back to an `NSWorkspace` reopen request instead of the deprecated `activateIgnoringOtherApps` path
- Unified logging can hide useful runtime details; file-based debug logs (`~/.config/Wink/debug.log`) are more reliable for diagnosis

## Residual Risks
- The new capture split and toggle guarantees are covered by code-level tests and by the automated suite, but they still need the targeted macOS matrix above before we can claim runtime correctness for Safari-only toggle-off, standard-vs-Hyper parity, Home, Clock, System Settings, or timeout-stress behavior
- The new 2026-04-09 attempt/session diagnostics, launch attachment, and untracked-hide session ownership are implemented and test-covered, and Safari fresh-launch / relaunch / external-activation were manually validated on packaged `build/Wink.app`; broader app-matrix and standard-vs-Hyper parity work still remain
- Event tap recovery semantics are implemented with thresholded escalation and degraded reporting, but a reproducible on-device timeout-stress run is still needed to confirm the live logs are operationally sufficient
- Signed/notarized release validation is still blocked on Developer ID availability; until those credentials exist, only the internal-package DMG artifact path can be verified end-to-end

## Immediate Next Actions
1. Keep the intended shortcut fixture explicit before interpreting readiness logs. Read the live `shortcuts.json` first, then match the expected readiness snapshot (`carbon=true`, `eventTap=true`, or both) to that saved route mix instead of assuming the old Safari/IINA baseline.
2. Expand the targeted macOS validation matrix from the now-confirmed Safari fresh-launch / relaunch / external-activation paths to Finder, Terminal, Home, Clock, System Settings, hidden/minimized paths, and event-tap timeout stress
3. Verify standard-shortcut vs Hyper parity on the same target apps beyond the already revalidated Safari cases, and capture any remaining app-specific exceptions in this file
4. Use the internal-package workflow for tester-facing DMG artifacts until Developer ID credentials are available
5. Run the DMG release workflow with real Developer ID and notary credentials on a `v*` tag once those credentials exist
6. Validate the published DMG on a clean macOS machine and confirm drag-install to `/Applications`
7. Fold any new validation findings back into this note, not into the feature overview docs
